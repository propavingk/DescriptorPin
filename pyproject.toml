[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "descriptorpin"
version = "1.0.1"
description = "Defensive integrity monitor for Model Context Protocol tool descriptors."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "DescriptorPin contributors"}]
